// import {ApplicationState} from '@stores/types';

// export const getAppState = (state: ApplicationState) => state.appState;

// export const getLoading = (state: ApplicationState) => state.appState.loading;

// export const getIndicator = (state: ApplicationState) =>
//   state.appState.progress_indicator;

// export const getInfoMessage = (state: ApplicationState) =>
//   state.appState.infoMessage;
